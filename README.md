# 运行需要
```
yum install python3 gem
gem install rake jekyll
./script/bootstarp
rake post title="hello world" #创建新文章
python3 run.py #生成分类
python3 gen_pages.py #生成标签
jekyll serve -H 0.0.0.0 -P 80 # 运行项目
```

